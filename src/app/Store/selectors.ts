// import { createFeatureSelector, createSelector } from '@ngrx/store';
// import { AuthState, mealFetchState, initialMealState } from './state';

// export const selectAuthState = createFeatureSelector<AuthState>('auth');
// export const selectMealState = createFeatureSelector<mealFetchState>('meal');
// // export const selectMealState = createFeatureSelector<
// //  mealFetchState
// // >('meal');

// export const selectUser = createSelector(
//   selectAuthState,
//   (state) => state.user
// );
// export const selectError = createSelector(
//   selectAuthState,
//   (state) => state.error
// );
// export const selectMeal = createSelector(
//   selectMealState,
//   (state) => state.meal
// );
// export const selectMealError = createSelector(
//   selectMealState,
//   (state) => state.error
// );
// export const selectMealIsLoading = createSelector(
//   selectMealState,
//   (state) => state.isLoading
// );

// export const selectData = createSelector(
//   selectMealState,
//   (state) => state.meal
// );
